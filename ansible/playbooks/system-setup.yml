---
# system-setup.yml

- name: Setup Debian system for Docker deployments
  hosts: linux
  become: true

  tasks:
    - name: Set fact for Docker-enabled hosts
      ansible.builtin.set_fact:
        is_docker_enabled_host: >-
          {{ docker_enabled_groups is defined and
             (docker_enabled_groups | intersect(group_names) | length > 0) }}
      tags: [always]

    - name: Set system timezone
      community.general.timezone:
        name: "{{ system_timezone }}"
      tags: [system]

    - name: Disable IPv6 on the host
      ansible.posix.sysctl:
        name: "{{ item.name }}"
        value: "{{ item.value }}"
        state: present
        sysctl_set: true
        reload: true
        sysctl_file: "/etc/sysctl.d/99-local.conf"
      loop:
        - { name: "net.ipv6.conf.all.disable_ipv6", value: "1" }
        - { name: "net.ipv6.conf.default.disable_ipv6", value: "1" }
      notify: Restart Docker
      tags: [system]

    - name: Update apt cache
      ansible.builtin.apt:
        update_cache: true
        cache_valid_time: 3600
      tags: [system]

    - name: Install required system packages
      ansible.builtin.apt:
        name: "{{ debian_packages }}"
        state: present
      tags: [system]

    - name: Create ansible service user
      ansible.builtin.user:
        name: "{{ ansible_service_user }}"
        groups: "{{ ansible_service_user_groups }}"
        shell: /bin/bash
        create_home: true
        state: present
      tags: [user]

    - name: Set up authorized keys for ansible user
      ansible.posix.authorized_key:
        user: "{{ ansible_service_user }}"
        key: "{{ item }}"
        state: present
      loop: "{{ ansible_ssh_public_keys }}"
      tags: [user]

    - name: Allow ansible user to have passwordless sudo
      ansible.builtin.lineinfile:
        path: /etc/sudoers.d/ansible
        line: "{{ ansible_service_user }} ALL=(ALL) NOPASSWD: ALL"
        state: present
        mode: "0440"
        create: true
        validate: "visudo -cf %s"
      tags: [user]

    - name: Run Coral Edge TPU setup role
      ansible.builtin.include_role:
        name: coral_edgetpu
      tags: [edgetpu]
      when: inventory_hostname == coral_edgetpu_host

    - name: Create directory for Docker GPG key
      ansible.builtin.file:
        path: /etc/apt/keyrings
        state: directory
        mode: "0755"
      tags: [docker]
      when: is_docker_enabled_host

    - name: Download Docker GPG key
      ansible.builtin.get_url:
        url: https://download.docker.com/linux/debian/gpg
        dest: /etc/apt/keyrings/docker.asc
        mode: "0644"
      tags: [docker]
      when: is_docker_enabled_host

    - name: Add Docker repository
      ansible.builtin.apt_repository:
        repo: "deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/debian {{ ansible_facts.distribution_release }} stable"
        state: present
        filename: docker
      tags: [docker]
      when: is_docker_enabled_host

    - name: Install Docker
      ansible.builtin.apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-buildx-plugin
          - docker-compose-plugin
        state: present
        update_cache: true
      tags: [docker]
      when: is_docker_enabled_host

    - name: Ensure Docker service is running
      ansible.builtin.systemd:
        name: docker
        state: started
        enabled: true
      tags: [docker]
      when: is_docker_enabled_host

    - name: Ensure ansible user is in docker group (refresh)
      ansible.builtin.user:
        name: "{{ ansible_service_user }}"
        groups: "{{ ansible_service_user_groups }}"
        append: true
      tags: [docker]
      when: is_docker_enabled_host

    - name: Create base directory for docker-compose projects
      ansible.builtin.file:
        path: "{{ docker_compose_base_dir }}"
        state: directory
        mode: "0755"
      tags: [docker]
      when: is_docker_enabled_host

    - name: Create Docker network for inter-container communication
      community.docker.docker_network:
        name: "{{ docker_network_name }}"
        state: present
      tags: [docker]
      when: is_docker_enabled_host

  handlers:
    - name: Restart Docker
      ansible.builtin.systemd:
        name: docker
        state: restarted
      when: is_docker_enabled_host
